In Baby Battle Bash at C:\Users\Nicky\Documents\baby-battle-bash

Read CLAUDE.md and res://Scripts/gamer_manager_node.gd first.

Fix two bugs in field visual display. Smallest safe patch only.

Bug 1 — Red buff does not update field card visuals:
In play_color_or_word_card(), after the Red-style ATK buff branch, refresh the field visuals so the displayed ATK on field cards updates immediately.

Bug 2 — Field animals shift position after End Turn:
In spawn_field_visuals(), the current loop is likely rebuilding positions from sequential order instead of preserving exact slot placement.
Fix it so slot 0 always maps to left, slot 1 to center, slot 2 to right using field_slots as the source of truth.

Implementation details:
- Use the real current field data structure already present in gamer_manager_node.gd
- If get_field_animals() already exists, use it
- If not, use field_slots / field_animals directly instead of inventing a new helper
- Preserve the same visual centering/alignment the cards already use in the working combat scene
- Do not introduce a new offset mistake while fixing slot order

Target spawn_field_visuals() pattern:
func spawn_field_visuals():
    for visual in field_visuals:
        visual.queue_free()
    field_visuals.clear()

    var field_node = get_tree().root.get_node("CombatScene/PlayerField_Node2D")
    var slot_positions = {
        0: Vector2(-200, 0),
        1: Vector2(0, 0),
        2: Vector2(200, 0),
    }

    for slot_key in field_slots:
        if field_slots[slot_key] != null:
            var card_node = CardScene.instantiate()
            field_node.add_child(card_node)
            card_node.setup(field_slots[slot_key])
            card_node.position = slot_positions[slot_key]
            field_visuals.append(card_node)

Important:
- Do not touch reward, lose, menu, animal select, GameState, or card drag logic
- Keep the patch minimal

After the patch:
1. List every file changed
2. Give a short test plan
3. Confirm whether Run Away draw behavior still needs separate testing