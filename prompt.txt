In Baby Battle Bash at C:\Users\Nicky\Documents\baby-battle-bash
Read CLAUDE.md, res://Scripts/roadmap_scene.gd, and res://Scripts/GameState.gd first.
Problem: All combat encounters after Fight 1 show Big Bear instead of progressing through Swift Fox and Stone Turtle. The encounter_number is not incrementing correctly per roadmap node.
Verify how encounter_number is being set when each combat node is clicked on the roadmap. It is likely hardcoded to 2 for all nodes after node 0.
Fix the roadmap so each combat node sets GameState.encounter_number based on which node it is:

Node 0 (Fight 1) = encounter_number 1
Node 1 or 2 depending on route (Fight 2) = encounter_number 2
Node 3 (Fight 3) = encounter_number 3
Node 4 (Boss) = encounter_number 4

The encounter_number should map directly to the node index or position in the route so each fight loads a different mob.
Do not touch combat logic, reward screen, lose screen, rest site, chest site, animal select, main menu, card drag, or relic systems.
List what changed and give a short test plan.