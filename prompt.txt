In Baby Battle Bash at C:\Users\Nicky\Documents\baby-battle-bash

Read these files before making any changes:
1. CLAUDE.md
2. res://Scripts/GameState.gd
3. res://Scripts/reward_screen.gd — read _ready(), _on_card_chosen(), and the gold_earned variable

---

GOAL:
Add real persistent gold to GameState. Award gold on combat win.
Display current gold on the reward screen. Smallest safe patch only.

CHANGES — two files only:

FILE 1: res://Scripts/GameState.gd

Add a new variable after is_boss_fight:
    var gold: int = 0

In reset_run(), add:
    gold = 0

FILE 2: res://Scripts/reward_screen.gd

Change 1 — In _ready(), update the gold label to show current gold
plus the amount being earned:

Replace:
    gold_label.text = "Gold: +" + str(gold_earned)

With:
    gold_label.text = "Gold: " + str(GameState.gold) + "  (+" + str(gold_earned) + ")"

This shows something like "Gold: 20  (+10)" so the player sees
their total AND what they're about to earn.

Change 2 — In _on_card_chosen(), BEFORE the GameState.reward_cards
append line, add:

    GameState.gold += gold_earned
    print("Gold earned: ", gold_earned, " | Total gold: ", GameState.gold)

This awards gold exactly once per combat win — _on_card_chosen fires
once then the scene changes, so there is no duplicate risk.

Do NOT remove the existing gold_earned variable — it still defines
how much gold this combat awards.

RULES:
- Only edit GameState.gd and reward_screen.gd
- Do not touch any other file
- Do not change pick_rewards()
- Do not change card pool definitions
- Do not change how _on_card_chosen adds cards to reward_cards
- Do not change nodes_completed logic
- Do not change scene transition at the end of _on_card_chosen
- Do not change combat, boss, win_combat, lose_combat
- Do not change roadmap, animal select, main menu, lose screen
- Do not create shop scene or shop script
- Smallest safe patch only

AFTER THE FIX, REPORT:
1. Show the full updated GameState.gd
2. Show the updated _ready() function from reward_screen.gd
3. Show the updated _on_card_chosen() function from reward_screen.gd
4. Confirm gold_earned variable is preserved
5. Confirm no other files were touched