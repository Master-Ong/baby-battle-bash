In Baby Battle Bash at C:\Users\Nicky\Documents\baby-battle-bash
Read CLAUDE.md, res://Scripts/GameState.gd, res://Scripts/lose_screen.gd, and res://Scene/lose_screen.tscn first.
Upgrade the lose screen with New Run and Main Menu buttons. Smallest safe patch only.
In res://Scripts/GameState.gd add a reset function:
gdscriptfunc reset_run():
    selected_starter = ""
    reward_cards = []
    relics = []
    player_hp = 50
    encounter_number = 1
    nodes_completed = []
    current_node = 0
In res://Scripts/lose_screen.gd update the button layout:
Replace the existing single Retry button with two buttons:

Button named NewRunButton with text "New Run"
Button named MainMenuButton with text "Main Menu"

On NewRunButton pressed:
gdscriptGameState.reset_run()
get_tree().paused = false
get_tree().change_scene_to_file("res://Scene/animal_select.tscn")
queue_free()
On MainMenuButton pressed:
gdscriptGameState.reset_run()
get_tree().paused = false
get_tree().change_scene_to_file("res://Scene/main_menu.tscn")
queue_free()
In res://Scene/lose_screen.tscn update the layout to show both buttons clearly. Keep the Defeat title and dark overlay. Stack the two buttons vertically centered on screen with clear separation.
Do not touch combat logic, reward screen, roadmap, chest site, rest site, animal select, or card drag logic.
List every file changed, explain how New Run works, and give a short test plan.